export default function ConfiguracionPage() {
  return (
    <div className="space-y-4 animate-fade-in">
      <div>
        <h2 className="text-xl font-semibold text-teal">Configuración</h2>
        <p className="text-sm text-muted-foreground">Gestión de usuarios, roles y parámetros del sistema</p>
      </div>
      <div className="bg-card rounded-lg card-shadow p-12 text-center">
        <p className="text-muted-foreground">Módulo de configuración — próximamente</p>
        <p className="text-sm text-muted-foreground mt-1">Gestión de usuarios y roles (ADMIN, TESORERO, APROBADOR, CONSULTA).</p>
      </div>
    </div>
  );
}
